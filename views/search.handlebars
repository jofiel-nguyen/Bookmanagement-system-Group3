<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Trackr - Search Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="stylesheet" type="text/css" href="style.css">
</head>
<body>
  <div class="container">
    <h1 class="text-center mt-5">Search Results</h1>
    {{#if books.length}}
      <div class="book-list">
        <div class="row">
          {{#each books}}
            <div class="col-lg-6">
              <div class="card">
                <div class="row no-gutters">
                  <div class="col-md-4">
                    <img src="{{this.bookImg}}" class="card-img" alt="Book Cover">
                  </div>
                  <div class="col-md-8">
                    <div class="card-body">
                      <h5 class="card-title">{{this.title}}</h5>
                      <p class="card-text">Author: {{this.author}}</p>
                      <p class="card-text">Publisher: {{this.publisher}}</p>
                      <a href="{{this.bookLink}}" target="_blank" class="btn btn-secondary">Read Book</a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          {{/each}}
        </div>
      </div>
    {{else}}
      <p class="text-center">No results found.</p>
    {{/if}}
  </div>
</body>
</html>
